package com.shiftplanner.solver.core.scheduling.values;

import com.shiftplanner.solver.core.utils.values.LongId;

public class TimetableId extends LongId {
  public TimetableId(Long id) {
    super(id);
  }
}
