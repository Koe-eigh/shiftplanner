package com.shiftplanner.solver.core.scheduling.values;

import com.shiftplanner.solver.core.utils.values.PersonName;

public class TeacherName extends PersonName {
  public TeacherName(String firstName, String lastName) {
    super(firstName, lastName);
  }
}

