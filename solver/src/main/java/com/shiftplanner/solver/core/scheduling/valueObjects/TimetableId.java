package com.shiftplanner.solver.core.scheduling.valueObjects;

import com.shiftplanner.solver.core.utils.StringId;

public class TimetableId extends StringId {
  public TimetableId(String id) {
    super(id);
  }
}
